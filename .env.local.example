# Copie ce fichier en .env.local (non commité) pour le dev local hors Docker (npm run dev).
# En Docker, ces valeurs sont injectées via compose.yaml / le build (voir Dockerfile).

BACKEND_INTERNAL_URL=http://localhost:8081
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
SITE_URL=http://localhost:3000
